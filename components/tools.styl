/* align */
Align(left)
Align(center)
Align(right)

/* float */
Float(left)
Float(right)

/* Clearfix */
.clearfix
  {'*zoom '}1
  &:before,
  &:after
    display table
    content ''
  &:after
    clear both

/* 水平居中 */

//如果有一个参数，没有初始值时不能在调用时不传参，不传会报错，如Center(flag) Center()则不行，可以将flag初始为为false，像这样Center(flag=false),再在包含代码里用if语句设置==false就可以默认不传参的享受初始flag为false时的代码块
Center()

/* 隐藏出现 */
ShowHide()